import { Box, Typography } from '@mui/material';

export const ModeloPrediccionesContent = () => {
  return (
    <Box>
      <Typography variant="h6" sx={{ mb: 2 }}>
        Modelo y Predicciones
      </Typography>
      <Typography color="text.secondary">
        Aquí irá el contenido de modelos y predicciones (formularios, visualizaciones, etc.)
      </Typography>
    </Box>
  );
};
